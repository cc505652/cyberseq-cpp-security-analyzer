// Tiny C Valid Sample Source Program
/* Multi-line header block comment
   Demonstrating language features for Tiny C Lexer evaluation */

int main() {
    // Variable Declarations & Data Types
    int count = 10;
    float pi = 3.14159;
    char newline = '\n';
    string greeting = "Hello, Secure World!";
    bool is_active = true;
    const int MAX_LIMIT = 999;

    // Compound Assignment & Increments
    count += 5;
    count++;

    // Security Built-in Function Call
    char buffer[256];
    gets(buffer);

    if (count > MAX_LIMIT || !is_active) {
        print("Limit reached!");
        return 0;
    } else {
        string query = "SELECT * FROM users WHERE id = 1";
        db_query(query);
    }

    return 0;
}
